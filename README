To do:

APP Layer:

-File transfer
  -Client side is built, but will need testing once server side is done.
  -Added logic to place file to upload/get in string 'filename'. Passed to functions
  -Added send_history function (SERVER). Will make use of file transfer for 'chat_history.txt'
-Commands: history, upload, get
-Message Cutting, NOT WORKING, FUCK!!

DL Layer:
-Create debug messaging mode
-Test more multi-client stuff
-Clean up comments/code

PHY Layer:
-Create debug messaging mode
-Clean up comments/code

PACKET LOSS
